# LicitaBot - Sistema de Inteligência em Licitações

## 🚀 Início Rápido

Este projeto é composto por um frontend em Vue.js 3 e um backend em Node.js.

### Requisitos
- Node.js 20+
- Docker & Docker Compose (Recomendado)

### Rodando com Docker
```bash
docker-compose up --build
```

### Rodando Manualmente

#### Backend
1. Entre na pasta `backend`
2. Instale as dependências: `npm install`
3. Configure o `.env` (MongoDB local necessário)
4. Inicie: `npm run dev`

#### Frontend
1. Entre na pasta `frontend`
2. Instale as dependências: `npm install`
3. Inicie: `npm run dev`

## 🛠️ Tecnologias
- **Frontend:** Vue 3, PrimeVue 4 (Aura Theme), PrimeFlex.
- **Backend:** Node.js, Express, Mongoose (MongoDB).
- **Design:** Moderno, responsivo e focado em UX para licitantes.

## 📁 Estrutura do Projeto
- `backend/`: API Express e modelos de dados.
- `frontend/`: Aplicação Vue e componentes UI.
- `prd.md`: Documentação de requisitos do produto.
